services:
  server-main:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    init: true
    volumes:
      - ../data:/usr/project/data
  server-test:
    build:
      context: .
      dockerfile: test.Dockerfile
