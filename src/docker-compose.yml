services:
  server-main:
    build:
      context: ./server
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    init: true
    volumes:
      - ../data:/usr/project/data
  server-test:
    build:
      context: ./server
      dockerfile: test.Dockerfile
